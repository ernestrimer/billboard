<h1><%= @song.name  %>Songs</h1>

<ul>
  <%= @songs.each do |song| %>
    <li>
      <%= link_to song.name, [@rank, song]
    </li>

  <% end %>
</ul>

<%= link_to 'Add new song', new_rank_song_path(@rank) %>
