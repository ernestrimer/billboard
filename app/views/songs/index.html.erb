<h1><%= @song.name  %>Songs</h1>

<ul>
  <%= @songs.each do |song| %>
    <li>
      <%= link_to song.name, [@artist, song]
    </li>

  <% end %>
</ul>

<%= link_to 'Add New Song', new_artist_song_path(@artist) %>
