<h1>Edit Song/h1>

<%= form_with(model: @rank, local: true, class: "ui form") do |f|%>
<div class = "field">
  <%= f.label :name%>
  <%= f.text_field :name, required: true %>
</div>

<div class = "field">
  <%= f.label :duration %>
  <%= f.text_field :duration, required: true %>
</div>

<%= f.text_field :duration, required: true %>
#  f.submit nil, class: 'ui green button' %>
  #ui green button from style library
<% end %>

# can copy into edit also
# t.string "name"
#     t.integer "duration"
#     t.bigint "artist_id",
